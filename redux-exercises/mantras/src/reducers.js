
export function mantra(state=[], action) {
    const newState = [ ...state ];
    switch(action.type) {
        case ADD_MANTRA:
            // do soomething??
            break;
        default:
            break;
    }
    return newState;
}